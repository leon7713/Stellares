package pageObject;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class OurComSection {
    public WebDriver driver;

    public OurComSection(WebDriver driver) {
        this.driver = driver;
    }
}
